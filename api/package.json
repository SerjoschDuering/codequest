{
  "name": "codequest-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply codequest-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply codequest-db --remote"
  },
  "dependencies": {
    "better-auth": "^1.4.18",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.9",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260210.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.64.0"
  }
}
